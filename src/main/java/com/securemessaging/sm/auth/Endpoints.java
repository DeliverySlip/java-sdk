package com.securemessaging.sm.auth;

/**
 * Endpoint is a helper class to isolate endpoint URLs used by the SDK. This allows the Endpoints to be accessed
 * from anywhere in the SDK and allow only a single area needing change to change the Endpoints data
 */
public class Endpoints {

    public static final String CCCAPI = "https://api.secure-messaging.com/api";


}
